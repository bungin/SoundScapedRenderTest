{
  "compilerOptions": {
    /* Language Options */
    "target": "ES5",                           /* Specify ECMAScript target version (ES5 for broad compatibility). */
    "lib": ["dom", "dom.iterable", "esnext"],  /* Include useful built-in JavaScript libraries (DOM, ESNext). */
    
    /* JavaScript Support */
    "allowJs": true,                           /* Allow JavaScript files to be compiled. */
    
    /* Type Checking */
    "strict": true,                            /* Enable all strict type-checking options. */
    "skipLibCheck": true,                      /* Skip type checking of all declaration files. */
    "forceConsistentCasingInFileNames": true,  /* Ensure consistent casing in file names. */
    
    /* Module Resolution */
    "module": "esnext",                        /* Use ESNext for module transpilation. */
    "moduleResolution": "node",                /* Use Node.js-style module resolution. */
    "esModuleInterop": true,                   /* Emit helpers for interop between ES Modules and CommonJS. */
    "allowSyntheticDefaultImports": true,      /* Allow default imports from modules without default exports. */
    "resolveJsonModule": true,                 /* Include support for importing JSON files. */
    "isolatedModules": true,                   /* Treat each file as a separate module. */
    
    /* JSX Support */
    "jsx": "react",                            /* Enable React JSX syntax support. */

    /* File Handling */
    "noEmit": true,                            /* Do not emit compiled JavaScript files. */
    
    /* Directories */
    "rootDir": "src",                          /* Specify the root folder of your source files. */
    "outDir": "dist"                           /* Specify the output folder for emitted files (if emitting). */
  },
  "include": ["src/**/*"],                      /* Include all files within the src folder. */
  "typeRoots": ["./node_modules/@types", "./src/types"] /* Add custom type definitions and @types modules. */
}
